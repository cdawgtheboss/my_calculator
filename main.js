const calcArr = [];

function updateDisplay(value) {
    //var calcArr = [];
    let newArr = calcArr.push(value);
    console.log(calcArr);
};

function equalsButton() {
    console.log(newArr);
};

function clearButton() {
    
}